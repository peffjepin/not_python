def resolve(v1: int, v2: int) -> Function[[Function[[int, int], int]], int]:
    def wrapper(fn: Function[[int, int], int]) -> int:
        return fn(v1, v2)
    return wrapper


@resolve(10, 20)
def example(v1: int, v2: int) -> int:
    return v1 * v2


# example should now be an integer == 10 * 20
print(example)
