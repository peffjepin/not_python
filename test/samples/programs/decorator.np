def wrapper(fn: Function[[int], int]) -> Function[[int], int]:
    def inner(val: int) -> int:
        return fn(val) * 10
    return inner


@wrapper
def decorated(val: int) -> int:
    return val + 1


print(decorated(1))
